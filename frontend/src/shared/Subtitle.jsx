import React from 'react';
import '../styles/Sub-title.css';

const Subtitle = ({ subtitle }) => {
    return (
        <h3 className='section_subtitle'>{subtitle}</h3>
    );
};

export default Subtitle;